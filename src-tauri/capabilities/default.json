{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-runtime-status",
  "description": "Allow the local main window to query sanitized runtime status only.",
  "windows": ["main"],
  "local": true,
  "platforms": ["windows"],
  "permissions": ["allow-runtime-status"]
}
